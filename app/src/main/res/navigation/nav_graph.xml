<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/reels">

    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.test.homework5.SettingsFragment"
        android:label="SettingsFragment"
        tools:layout="@layout/settings" />

    <fragment
        android:id="@+id/reels"
        android:name="com.test.homework5.ReelsFragment"
        android:label="ReelsFragment"
        tools:layout="@layout/reels">
        <action
            android:id="@+id/action_reelsFragment_to_settingsFragment"
            app:destination="@id/settingsFragment" />
    </fragment>

</navigation>