<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/auth">

    <fragment
        android:id="@+id/searchFragment"
        android:name="com.test.homework5.SearchFragment"
        android:label="SettingsFragment"
        tools:layout="@layout/search" />

    <fragment
        android:id="@+id/auth"
        android:name="com.test.homework5.AuthFragment"
        android:label="ReelsFragment"
        tools:layout="@layout/authorization">
        <action
            android:id="@+id/action_authFragment_to_searchFragment"
            app:destination="@id/searchFragment" />
    </fragment>

</navigation>